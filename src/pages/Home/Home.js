import React, { useEffect } from 'react';
import { socket } from '../../socket';

const Home = ({ dispatch }) => {
  return (
    <div>
      <button>create room</button>
    </div>
  );
}

export default Home;
