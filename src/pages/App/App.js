import React from 'react';
import Routes from '../../routes';
import './App.scss';

const App = ({ user, room }) => {
  return (
    <div className="app">
      <Routes />
    </div>
  );
};

export default App;
